{
  "output_dir": "out/",
  "post_install": "ls",
  "build": "npm run build && npm run export",
  "branches": {
    "prod_env": "master:tag",
    "stage_env": "master"
    "dev_env": "develop"
  }

}

